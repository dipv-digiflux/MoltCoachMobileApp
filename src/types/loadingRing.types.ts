/**
 * Props for the LoadingRing circular indicator component.
 */
export type LoadingRingProps = {
  /** Ring size (width/height). Default 44. */
  size?: number;
  /** Border width. Default 2. */
  borderWidth?: number;
  /** Rotation animation duration in ms. Default 900. */
  durationMs?: number;
};
