import { RouteProp } from '@react-navigation/native';

import { AppStackParamList } from '@/types/navigation.types';

export type AddedClientsRouteProp = RouteProp<
  AppStackParamList,
  'AddedClients'
>;
