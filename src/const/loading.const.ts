/**
 * Default values for loading-related UI components.
 * Used by LoadingRing and GettingThingsReady.
 */

/** Default ring size (width/height in px) for LoadingRing. */
export const LOADING_RING_DEFAULT_SIZE = 44;

/** Default border width (px) for LoadingRing. */
export const LOADING_RING_DEFAULT_BORDER_WIDTH = 2;

/** Default rotation animation duration (ms) for LoadingRing. */
export const LOADING_RING_DEFAULT_DURATION_MS = 900;
